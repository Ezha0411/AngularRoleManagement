import { KeyvalueNoSortingPipe } from './keyvalue-no-sorting.pipe';

describe('KeyvalueNoSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyvalueNoSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
